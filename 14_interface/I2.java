interface I2 {
    // interface variables are implicitly public static final...
    // because the variables are implicitly final ... you must initialize the variable at the time of declaration..
    /*public static final*/ int y = 8;  // by default class level, final variable with public access control
}
