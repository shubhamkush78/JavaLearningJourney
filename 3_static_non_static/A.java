class A{
    //instance level variables (non-static variables)
    int x; //primitive 
    String y; //class type variable

    // class level variables (static variables)
    static float z; //primitive type var
    static Integer m; //class type variable

    void process (boolean j, Float e) {
        char r;
        Character k;
        //j, e, r, k are local levelvariables (static Not applicable)
        //j, r are primitive type variables
        //e, k are class type variables
    }
}